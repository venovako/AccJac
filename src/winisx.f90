PURE SUBROUTINE WINISX(M, N, G, LDG, V, LDV, SV, IX, GX, INFO)
#ifdef __GFORTRAN__
  USE, INTRINSIC :: ISO_C_BINDING, ONLY: c_long_double
#else
  USE, INTRINSIC :: ISO_FORTRAN_ENV, ONLY: REAL128
#endif
  IMPLICIT NONE
#ifdef __GFORTRAN__
  INTEGER, PARAMETER :: K = c_long_double
#else
  INTEGER, PARAMETER :: K = REAL128
#endif
  REAL(KIND=K), PARAMETER :: ZERO = 0.0_K, ONE = 1.0_K
  INTEGER, INTENT(IN) :: M, N, LDG, LDV
  COMPLEX(KIND=K), INTENT(IN) :: G(LDG,N)
  COMPLEX(KIND=K), INTENT(OUT) :: V(LDV,N)
  REAL(KIND=K), INTENT(OUT) :: SV(N), GX
  INTEGER, INTENT(OUT) :: IX(N), INFO
  REAL(KIND=K) :: X, Y
  INTEGER :: I, J
#include "hinisx.f90"
END SUBROUTINE WINISX
