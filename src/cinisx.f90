PURE SUBROUTINE CINISX(M, N, G, LDG, V, LDV, SV, IX, GX, INFO)
  USE, INTRINSIC :: ISO_FORTRAN_ENV, ONLY: REAL32
  IMPLICIT NONE
  INTEGER, PARAMETER :: K = REAL32
  REAL(KIND=K), PARAMETER :: ZERO = 0.0_K, ONE = 1.0_K
  INTEGER, INTENT(IN) :: M, N, LDG, LDV
  COMPLEX(KIND=K), INTENT(IN) :: G(LDG,N)
  COMPLEX(KIND=K), INTENT(OUT) :: V(LDV,N)
  REAL(KIND=K), INTENT(OUT) :: SV(N), GX
  INTEGER, INTENT(OUT) :: IX(N), INFO
  REAL(KIND=K) :: X, Y
  INTEGER :: I, J
#include "hinisx.f90"
END SUBROUTINE CINISX
